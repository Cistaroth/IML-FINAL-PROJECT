[project]
name = "IML-Final-Project"
version = "1.0.0"
description = "We attempt to classify whether a user will watch a show using a KNN architecture. We train on the Netflix Prize Dataset"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
